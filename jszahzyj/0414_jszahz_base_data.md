# 任务
请帮我修改 jszahzyj 的数据源逻辑，以及前端 [jszahz_topic_tab.html](jszahzyj/templates/jszahz_topic_tab.html) 页面中的过滤区控件。

## 一、数据源调整为 3 部分

### 1. 基础数据
其他两个数据源都基于这部分数据进行扩展：
- 在基础数据上增加标签字段。
- 如果其他两个数据源中的记录在基础数据里不存在，也要补充进去。

### 2. Excel 导入格式
基础数据通过 Excel 导入，要求如下：
- 必须包含 5 个 sheet，sheet 名称分别为：云城、云安、罗定、新兴、郁南。
- 各 sheet 对应的编码如下：
    - 云城：445302000000
    - 云安：445303000000
    - 罗定：445381000000
    - 新兴：445321000000
    - 郁南：445302000000
- 上述编码对应 `stdata.b_dic_zzjgdm` 表的 `ssfjdm` 字段，页面展示时使用 `ssfj`。
- 每个 sheet 只包含 3 列：`序号`、`姓名`、`证件号码`。
- 入库时请评估主键/唯一标识方案，比较 UUID、序号或其他方案，优先选择便于维护、便于幂等导入的方式。
- 最终保存时必须按 `证件号码` 去重。

### 3. 列管、分级数据
该部分数据来源于 `stdata.b_per_jszahzryxxwh` 表。
- 初始化查询时增加过滤条件：`WHERE p.sflg = '1' AND p.deleteflag = '0'`。
- 其中 `p.sflg = '1'` 表示“已列管”。

### 4. 其他标签类数据
以下数据的 Excel 导入逻辑保持不变：
- 服药情况
- 监护情况
- 既往有自杀或严重伤人
- 列为重点关注人员

## 二、前端过滤区调整
修改 [jszahz_topic_tab.html](jszahzyj/templates/jszahz_topic_tab.html) 中的过滤区控件：
- 删除“列管开始时间”和“列管结束时间”两个控件。
- 新增“列管”单选按钮。
- 默认选中“列管”，对应条件为 `sflg = '1'`。
- 取消勾选时，前端请求中移除该条件。

## 三、实现要求
- 保持现有页面结构和交互风格，尽量只做必要修改。
- 若涉及后端接口、查询条件或导入逻辑联动，请一并同步处理。
- 完成后请确保数据源、筛选条件和前端展示逻辑一致。